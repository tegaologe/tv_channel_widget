name: tv_channel_widget
description: ""
version: 0.0.1+1

publish_to: "none"

environment:
  sdk: ">=2.18.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  after_layout: ^1.2.0
  data_table_2: ^2.6.0
  flutter:
    sdk: flutter

  intl: ^0.20.2
  linked_scroll_controller: ^0.2.0

  super_sliver_list: ^0.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
